{
  "ignoreCommand": "if [ \"$VERCEL_ENV\" = \"production\" ] && ! echo \"$VERCEL_GIT_COMMIT_MESSAGE\" | grep -q '^chore: release v'; then exit 0; else exit 1; fi",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
