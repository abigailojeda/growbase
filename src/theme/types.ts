export interface ThemeColors {
  primary: string
  secondary: string
  background: string
  surface: string
  text: string
}

export interface Theme {
  light: ThemeColors
  dark: ThemeColors
}

export type ColorMode = 'light' | 'dark'

export interface ThemeColors {
  primary: string
  secondary: string
  background: string
  surface: string
  text: string
}

export interface Theme {
  light: ThemeColors
  dark: ThemeColors
}
